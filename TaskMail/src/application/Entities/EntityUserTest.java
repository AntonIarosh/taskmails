package application.Entities;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

class EntityUserTest {

	@Test
	final void testEntityUser() {
		EntityUser user = new EntityUser();
		user.setEmail(null);
		user.setIdPost(0);
		user.setFirstName(null);
		user.setSecondName(null);
		user.setLastName(null);
		user.setPassword(null);
		user.setNamePost(null);
		
		EntityUser userTest = new EntityUser();
		Assert.assertEquals(user.getIdPost(), userTest.getIdPost()); 
				
	}

	@Test
	final void testEntityUserStringStringStringStringStringInt() {
		EntityUser user = new EntityUser();
		
		user.setEmail("Email");
		user.setIdPost(1);
		user.setFirstName("FirstName");
		user.setSecondName("SecondName");
		user.setLastName("LastName");
		user.setPassword("Password");
		user.setNamePost("NamePost");
		
		EntityUser userTest = new EntityUser("FirstName","SecondName","LastName","Password","Email",1);
		
		Assert.assertEquals(user.getEmail(),userTest.getEmail());
		Assert.assertEquals(user.getIdPost(),userTest.getIdPost());
		Assert.assertEquals(user.getFirstName(),userTest.getFirstName());
		Assert.assertEquals(user.getSecondName(),userTest.getSecondName());
		Assert.assertEquals(user.getLastName(),userTest.getLastName());
		Assert.assertEquals(user.getPassword(),userTest.getPassword());
		Assert.assertEquals(user.getPassword(),userTest.getPassword());
	}

}*/
