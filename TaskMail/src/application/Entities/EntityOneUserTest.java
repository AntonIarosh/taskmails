package application.Entities;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

class EntityOneUserTest {

	@Test
	final void testEntityOneUser() {
		EntityOneUser user = new EntityOneUser();
		user.setEmail(null);
		
		EntityOneUser userTest = new EntityOneUser();
		Assert.assertFalse(userTest.equals(user));
	}
	
	@Test
	final void testEntityOneUserSG() {
		EntityOneUser user = new EntityOneUser();
		user.setEmail("em");
		user.setFirstName("fn");
		user.setLastName("lm");
		user.setSecondName("sn");
		user.setNamePost("np");
		user.setPassword("pass");
		
		Assert.assertEquals("em",user.getEmail());
		Assert.assertEquals("fn",user.getFirstName());
		Assert.assertEquals("lm",user.getLastName());
		Assert.assertEquals("sn",user.getSecondName());
		Assert.assertEquals("np",user.getNamePost());
		Assert.assertEquals("pass",user.getPassword());
	}

}*/
