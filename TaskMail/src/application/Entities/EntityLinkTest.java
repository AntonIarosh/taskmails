package application.Entities;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

class EntityLinkTest {

	@Test
	final void testEntityLink() {
		EntityLink link = new EntityLink();
		link.setIdLink(0);
		link.setLink(null);
		
		EntityLink linkTest = new EntityLink();
		Assert.assertEquals(link.getIdLink(),linkTest.getIdLink()); 
	}

	@Test
	final void testEntityLinkIntString() {
		EntityLink link = new EntityLink();
		link.setIdLink(1);
		link.setLink("Link");
		
		EntityLink linkTest = new EntityLink(1,"Link");
		
		Assert.assertEquals(link.getLink(),linkTest.getLink());
		Assert.assertEquals(link.getIdLink(),linkTest.getIdLink());
	}

}*/
