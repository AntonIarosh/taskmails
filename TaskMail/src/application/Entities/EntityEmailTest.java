package application.Entities;

/*import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

import javafx.beans.property.SimpleIntegerProperty;

class EntityEmailTest {

	@Test
	final void testEntityEmail() {
		EntityEmail email = new EntityEmail();
		email.setEmail(null);
		email.setIMAPserver(null);
		email.setSMPTserver(null);
		email.setPassword(null);
		
		EntityEmail emailTest = new EntityEmail();
		Assert.assertFalse(emailTest.equals(email));
	}
	
	@Test
	final void testEntityEmailAlls() {
		EntityEmail email = new EntityEmail();
		email.setEmail("em");
		email.setIMAPserver("im");
		email.setSMPTserver("sm");
		email.setPassword("pas");
		Assert.assertEquals("em",email.getEmail());
		Assert.assertEquals("im",email.getIMAPserver());
		Assert.assertEquals("sm",email.getSMPTserver());
		Assert.assertEquals("pas",email.getPassword());
	}
}*/
