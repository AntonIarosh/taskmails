package application.Entities;

/*import org.junit.Assert;
import org.junit.jupiter.api.Test;

class EntityCommentTest {

	@Test
	final void testEntityComment() {
		EntityComment comment = new EntityComment();
		comment.setComment(null);
		comment.setIdComment(0);
		
		EntityComment commentTest = new EntityComment();
		Assert.assertEquals(comment.getIdComment(),commentTest.getIdComment()); 
	}

	@Test
	final void testEntityCommentIntString() {
		EntityComment comment = new EntityComment();
		comment.setComment("Comment");
		comment.setIdComment(1);
		
		EntityComment commentTest = new EntityComment(1,"Comment");
		
		Assert.assertEquals(comment.getComment(),commentTest.getComment());
		Assert.assertEquals(comment.getIdComment(),commentTest.getIdComment());
	}

}*/
